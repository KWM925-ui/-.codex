# Current worktree file. The inserted comment block intentionally shifts line
# numbers relative to the clean snapshot copy below.
#
# The regression target is to patch this active file, not the snapshot.

def add(a, b):
    return a - b
